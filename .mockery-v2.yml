log-level: error

all: false
recursive: false

dir: '{{.InterfaceDir}}'
mockname: '{{.Mock}}{{.InterfaceName}}'
outpkg: '{{.PackageName}}'

packages:

  github.com/lucaspopp-wbd/mockery-issue:
    config:
      recursive: true
      all: true

  github.com/istreamlabs/go-sdk/isp:
    config:
      recursive: true
      all: true
      outpkg: mocks
      dir: mocks
    interfaces:
      ChannelsForOrganizationApi:
